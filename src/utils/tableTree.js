export function setTableTreeData (data = [], {expanded = '_expaneded', level = '_level', show = '_show', children = 'children'}) {
  for (let i = 0, i < data,length; i < len; i++) {

  }
}

function loop (data) {

}